pipeline {
    agent {
    	dockerfile {
        filename '02 - Dockerfile - Dockerfile Agent'
        dir '003 - Defining Execution Environments/02 - Dockerfile - Dockerfile Agent'
        label 'my-defined-label'
    }
	}
    stages {
        stage('build') {
            steps {
                sh 'if [ "$ENV_VARIABLE" != "Value for execution" ]; then exit 1; fi'
            }
        }
    }
}
